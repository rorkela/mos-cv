#include "../main.h"
#include "carrier.h"
void carrier_continuity(double *V, double *Vprev, double *nprev, double *pprev, double *n, double *p, int N, int isforhole)
{ 
  double *J=malloc(N*3*sizeof(double));
  double *R=malloc(N*sizeof(double));
  for(int i=0;i<N;i++)
  {
    
  }
}
compute_F(double *F, double *V, double *Vprev, double *nprev, double *pprev, double *n, double *p, int N, int is_n)
{
  
}
